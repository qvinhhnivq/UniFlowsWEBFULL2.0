import{getData,saveData}from'./data.js';if(sessionStorage.getItem('uniflows-artist')!=='true')location.replace('artist-login.html');const form=document.querySelector('#release-form'),list=document.querySelector('#release-list');let data=getData(),artist=data.artists[0];function slug(s){return String(s).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}function render(){list.innerHTML=artist.products.map((p,i)=>`<div class="queue-item"><span>${p.type||'Single'}</span><strong>${p.title}</strong><b>${p.submissionStatus||'Đã phát hành'}</b><a href="listen.html?artist=${artist.id}&release=${p.slug||slug(p.title)}">Smart Link ↗</a></div>`).join('')}form.onsubmit=e=>{e.preventDefault();let v=Object.fromEntries(new FormData(form));artist.products.unshift({title:v.title,type:`${v.type} · ${v.releaseDate}`,slug:slug(v.title),links:{},submissionStatus:'Đang chờ UniFLOWs duyệt',credits:v.credits,metadata:v});saveData(data);form.reset();render();alert('Đã gửi release vào hàng chờ duyệt.')};render();
